1) Replace NSDrawer w sth else? But with what?? Too slow

2) Retractor and Protractor looks weird

3) Add threshold so that modcom set alpha and sigma of 4 CPGs (is there a better way) w command neurons

